const { Error } = require('./error.model');
const { Subject } = require('./subject.model');
const { CodeExample } = require('./code-example.model');

module.exports = {
  Error,
  Subject,
  CodeExample
}